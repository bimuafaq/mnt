name: Build AOSP Module

on:
  workflow_dispatch:
    inputs:
      target:
        description: 'Module to build'
        type: choice
        required: true
        default: SystemUI
        options:
          - SystemUI
          - TrebuchetQuickStep
          - Settings
          - org.lineageos.platform SystemUI LineageParts LineageSettingsProvider

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Inspect storage & free up disk space
        run: |
          echo "==== df -hT ===="
          df -hT

          echo
          echo "==== lsblk ===="
          lsblk -f

          echo
          echo "==== findmnt ===="
          findmnt -D

          echo
          echo "==== Candidate writable dirs ===="
          for d in / /mnt /tmp /dev/shm /var/tmp /opt /home/runner /home/runner/work "$RUNNER_TEMP" "$GITHUB_WORKSPACE"; do
            [ -z "$d" ] && continue
            if [ -d "$d" ]; then
              free=$(df -h "$d" 2>/dev/null | awk 'NR==2{print $4" free on "$1" ("$6")"}')
              if touch "$d/.write_test" 2>/dev/null; then
                writable="WRITABLE (no sudo)"
                rm -f "$d/.write_test"
              elif sudo -n touch "$d/.write_test" 2>/dev/null; then
                writable="writable via sudo"
                sudo rm -f "$d/.write_test"
              else
                writable="NOT writable"
              fi
              printf '%-30s  %-22s  %s\n' "$d" "$writable" "$free"
            fi
          done

          echo
          echo "==== Top 20 biggest preinstalled dirs ===="
          sudo du -h -d1 /usr/local /opt /usr/share 2>/dev/null | sort -rh | head -20

          echo
          echo "==== /usr/local/lib breakdown ===="
          sudo du -h -d2 /usr/local/lib 2>/dev/null | sort -rh | head -30

          echo
          echo "==== Before cleanup ===="
          df -h /

          sudo rm -rf \
            /usr/share/dotnet \
            /usr/local/.ghcup \
            /usr/share/swift \
            /usr/local/julia* \
            /usr/share/miniconda \
            /opt/az \
            /usr/share/az_* \
            /opt/microsoft \
            /opt/google \
            /usr/local/aws-cli \
            /opt/pipx

          echo
          echo "==== After cleanup ===="
          df -h /

      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build packages
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq \
            bc bison build-essential ccache curl flex g++-multilib gcc-multilib \
            git git-lfs gnupg gperf imagemagick protobuf-compiler python3-protobuf \
            lib32readline-dev lib32z1-dev libdw-dev libelf-dev libgnutls28-dev lz4 \
            libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop pngcrush rsync \
            schedtool squashfs-tools xsltproc xxd zip zlib1g-dev \
            lib32ncurses5-dev libncurses5 libncurses5-dev
          sudo apt-get install -y -qq python-is-python3

      - name: Cache ccache
        uses: actions/cache@v5
        with:
          path: ~/.ccache
          key: ccache-lineage-18.1-${{ github.run_id }}
          restore-keys: |
            ccache-lineage-18.1-

      - name: Setup ccache
        run: |
          mkdir -p ~/.ccache
          ccache -M 5G
          ccache -o compression=true
          ccache -z
          echo "USE_CCACHE=1" >> $GITHUB_ENV
          echo "CCACHE_DIR=$HOME/.ccache" >> $GITHUB_ENV
          echo "CCACHE_EXEC=$(command -v ccache)" >> $GITHUB_ENV
          echo "CCACHE_COMPRESS=1" >> $GITHUB_ENV

      - name: Install repo
        run: |
          sudo wget -q https://storage.googleapis.com/git-repo-downloads/repo -O /usr/local/bin/repo
          sudo chmod a+x /usr/local/bin/repo
          repo --version

      - name: Git config
        run: |
          git config --global user.name "ci"
          git config --global user.email "ci@ci.com"
          git lfs install
          git config --global trailer.changeid.key "Change-Id"

      - name: Repo init
        run: |
          repo init --depth=1 --no-clone-bundle --partial-clone --clone-filter=blob:none \
            --manifest-depth=1 -g default,-mips,-darwin,-notdefault \
            -u https://github.com/LineageOS/android.git -b lineage-18.1
          mkdir -p .repo/local_manifests
          cp local_manifest.xml .repo/local_manifests/

      - name: Repo sync
        run: |
          repo sync --force-sync -c --no-clone-bundle --no-tags \
            --optimized-fetch --prune --fail-fast -j$(nproc)

      - name: Setup vendor keys
        env:
          VENDOR_KEYS: ${{ secrets.VENDOR_KEYS }}
        run: |
          echo "$VENDOR_KEYS" | base64 -d > vendor_keys.zip
          unzip -o vendor_keys.zip
          rm -f vendor_keys.zip
          export OWN_KEYS_DIR="$PWD/vendor_keys"
          sudo ln -sf "$OWN_KEYS_DIR/releasekey.pk8" "$OWN_KEYS_DIR/testkey.pk8"
          sudo ln -sf "$OWN_KEYS_DIR/releasekey.x509.pem" "$OWN_KEYS_DIR/testkey.x509.pem"
          echo "OWN_KEYS_DIR=$OWN_KEYS_DIR" >> $GITHUB_ENV

      - name: Build
        env:
          TARGET: ${{ inputs.target }}
        run: |
          source build/envsetup.sh
          lunch lineage_RMX2185-user
          make $TARGET -j$(nproc)

      - name: Package & Upload to Telegram
        env:
          TARGET: ${{ inputs.target }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          source build/envsetup.sh
          lunch lineage_RMX2185-user

          VERSION=$(date +%Y%m%d-%H%M)

          tg_upload() {
            curl -sf -F "chat_id=${TELEGRAM_CHAT_ID}" \
                 -F "caption=${TARGET} | ${VERSION}" \
                 -F "document=@$1" \
                 -F "parse_mode=HTML" \
                 -F "disable_web_page_preview=true" \
                 "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument" > /dev/null
          }

          write_module_prop() {
            printf 'id=system_push_test\nname=System Test\nversion=%s\nversionCode=%s\nauthor=system\ndescription=System Test\n' \
              "$VERSION" "${VERSION//-/}" > module.prop
          }

          case "$TARGET" in
            TrebuchetQuickStep)
              cd "$OUT/system/system_ext/priv-app/TrebuchetQuickStep"
              zip -r launcher3.zip TrebuchetQuickStep.apk
              tg_upload launcher3.zip
              ;;
            Settings)
              cd "$OUT/system/system_ext/priv-app/Settings"
              zip -r Settings.zip Settings.apk
              tg_upload Settings.zip
              ;;
            SystemUI)
              cd "$OUT"
              ZIPNAME="systemui-${VERSION}.zip"
              write_module_prop
              zip -r "$ZIPNAME" module.prop system/system_ext/priv-app/SystemUI/SystemUI.apk
              tg_upload "$ZIPNAME"
              ;;
            "org.lineageos.platform SystemUI LineageParts LineageSettingsProvider")
              cd "$OUT"
              ZIPNAME="system-${VERSION}.zip"
              write_module_prop
              zip -r "$ZIPNAME" \
                module.prop \
                system/framework/org.lineageos.platform.jar \
                system/system_ext/priv-app/SystemUI/SystemUI.apk \
                system/priv-app/LineageParts/LineageParts.apk \
                system/priv-app/LineageSettingsProvider/LineageSettingsProvider.apk
              tg_upload "$ZIPNAME"
              ;;
          esac

      - name: Free space after
        run: df -h /

      - name: Source size
        run: |
          du -sh --exclude=.git

      - name: ccache stats
        if: always()
        run: ccache -s -v

